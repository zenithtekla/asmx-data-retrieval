MANEX_HTTP_REQ_ACCT_DATE = "http://erp-2/ews/ManexWebService.asmx/GetCustomerByCutOffDate?Acct_Date="
MANEX_HTTP_REQ_SO_WO = "http://erp-2/ews/ManexWebService.asmx/GetSalesOrderAndWorkOrder?WorkOrderNo="
MANTIS_QUERY_LAST_CUSTOMER_UPDATE = "SELECT MAX(DISTINCT(time_stamp)) FROM mantis_customer_table"
MANTIS_QUERY_LAST_SO_WO_UPDATE = "SELECT MAX(DISTINCT(time_stamp)) FROM mantis_plugin_serials_serial_table"

MANTIS_QUERY_CUSTOMER_FIND = "SELECT name FROM mantis_customer_table WHERE name = '%s'"

MANTIS_QUERY_WO_FIND = "SELECT wono as WO_NO, sono as SO_NO FROM mantis_wo_so_table WHERE wono = '%s' ORDER BY wono"

MANTIS_QUERY_SO_FIND = "SELECT sales_order as SO_NO FROM mantis_plugin_serials_serial_table WHERE sales_order = '%s' AND assembly_id= '%d' ORDER BY assembly_id"

MANTIS_QUERY_EXECUTE_UPDATE = "
	INSERT INTO mantis_customer_table
		( customer_id, customer_name, status, timestamp )
	VALUES
		( null, '%s', '%s', '%d' )"

MANTIS_QUERY_WO_FIND_RELEVANTS = "
	SELECT
		w.unique_key as UNIQ_KEY,
		w.wono as WO_NO,
		w.sono as SO_NO,
		a.number as ASSY_NO,
		a.revision as REVISION,
		c.name as CUST_NAME
	FROM mantis_wo_so_table w
	LEFT JOIN mantis_assembly_table a
		ON w.unique_key = a.unique_key
	LEFT JOIN mantis_customer_table c
		ON a.customer_id = c.id
	WHERE w.wono = '%s'
	ORDER BY w.wono
"

MANTIS_QUERY_INSERT_WO_TABLE = "
	INSERT INTO mantis_wo_so_table_test
		(id, wono, sono, quantity, due, unique_key)
	VALUES
		( null, '%s', '%s', '%d', '%d', '%s' )"
MANTIS_QUERY_UPDATE_CUSTOMER_TABLE = "ALTER TABLE mantis_customer_table
    ADD status BOOLEAN NOT NULL DEFAULT '1',
    ADD time_stamp int(10) unsigned NOT NULL DEFAULT '1';"

MANTIS_QUERY_SET_TIMESTAMP = "UPDATE mantis_customer_table SET time_stamp = UNIX_TIMESTAMP(now())"

MANTIS_QUERY_UPDATE_FROM_MANEX = "UPDATE mantis_wo_so_table_test SET quantity = '%d', due = '%d' WHERE unique_key = '%s'"
KARMA = TEST CASE
MOCHA_TEST = false;